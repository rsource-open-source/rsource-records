// This file will create a class where a "command" exists, and
// can be turned into both slash commands and regular typed commands.

// Creating a class that extends these, and will handle events of what gets outputted.
// Example: on error, return message "There was an internal error!"
// In slash commands, this will default to return not ephermal, however, there will
// be an optional boolean for that IF it is a slash command, along with timed deletes.

// https://github.com/orgs/rsource-open-source/projects/1#card-67974091
